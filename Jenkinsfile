pipeline {
    agent any

    stages {
        stage('compile') {
            steps {
                script {
                echo 'compile the code'
            }
        }
    }
           stage('unit test') {
            steps {
                script {
                echo 'test the code'
            }
        }
               stage('package') {
            steps {
                script {
                echo 'package the code'
            }
        }
    
    }
}
